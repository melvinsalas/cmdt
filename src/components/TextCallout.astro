---
type Variant = "warning" | "info" | "success";

type Props = {
    variant?: Variant;
    title: string;
};

const { variant = "warning", title }: Props = Astro.props;

const variantStyles: Record<Variant, { box: string }> = {
    warning: {
        box: "border-orange-500 bg-orange-50 text-orange-900",
    },
    info: {
        box: "border-sky-500 bg-sky-50 text-sky-900",
    },
    success: {
        box: "border-emerald-500 bg-emerald-50 text-emerald-900",
    },
};

const styles = variantStyles[variant];
---

<aside class:list={["my-3 border-l-4 p-4", styles.box]} role="alert">
	<strong class="mb-1 block font-semibold">{title}</strong>
	<div class="text-callout__content">
		<slot />
	</div>
</aside>

<style>
	.text-callout__content :global(:first-child) {
		margin-top: 0;
	}

	.text-callout__content :global(:last-child) {
		margin-bottom: 0;
	}
</style>
