---
import { YoutubeIcon } from '@hugeicons/core-free-icons';
import HugeIcon from './HugeIcon.astro';

interface Props {
	badges?: string[];
}

const { badges = [] } = Astro.props;

const iconDefinitions = {
	video: {
		icon: YoutubeIcon,
		label: 'Incluye video',
	},
};

const icons = badges.flatMap((badge) => {
	const definition = iconDefinitions[badge as keyof typeof iconDefinitions];
	return definition ? [definition] : [];
});
---

{
	icons.map(({ icon, label }) => (
		<span title={label} aria-label={label} class="inline-flex text-slate-500">
			<HugeIcon icon={icon} aria-hidden="true" class="h-4 w-4" />
		</span>
	))
}
